pipeline:
  docker:
    image: plugins/docker
    repo: aliakseimaniuk/docker-dotnet-matlab
    tags: latest
    secrets: [ docker_username, docker_password, docker_email, docker_repo ]
    when:
      branch: ["master"]
